# SKDF Traffic Accidents Parser (Playwright)
## Веселов Александр

Парсер данных о ДТП с сайта **[https://скдф.рф/traffic-accidents/](https://скдф.рф/traffic-accidents/)**
Работает с **динамическим React-интерфейсом**, использует **Playwright (Python)** и **календарь react-datepicker** для выбора периода.

---

## 📌 Возможности

* Автоматически:

  * открывает страницу ДТП
  * выбирает **все доступные поля таблицы**
  * открывает фильтры
  * раскрывает аккордеон **«Период»**
  * выбирает диапазон дат **через календарь (кликами)**
    👉 ручной ввод дат не используется (он не работает на сайте)
  * устанавливает **«Показывать по 160»**
  * постранично собирает данные
* Корректно ждёт перерисовку таблицы (без `sleep`)
* Сохраняет результат в CSV

---

## 🧱 Стек

* Python 3.10+ (проверено на 3.12)
* Playwright (sync API)
* pandas
* Chromium (устанавливается Playwright)

---

## 📦 Установка

### 1. Клонировать проект

```bash
git clone <repo_url>
cd <repo>
```

### 2. Виртуальное окружение

```bash
python -m venv .venv
source .venv/bin/activate  # macOS / Linux
# .venv\Scripts\activate   # Windows
```

### 3. Установить зависимости

```bash
pip install playwright pandas
playwright install
```

---

## ⚙️ Настройки

Все основные параметры находятся в начале файла `main.py`:

```python
START_DATE = date(2024, 11, 1)
END_DATE   = date(2024, 11, 30)

MAX_PAGES = 10          # сколько страниц таблицы собрать
PAGE_SIZE = "160"       # 20 | 40 | 80 | 160
HEADLESS = True         # False — для отладки
```

⚠️ **Важно**
Дата задаётся **как `datetime.date`**, а не строкой — календарь выбирается кликами.

---

## ▶️ Запуск

```bash
python main.py
```

После завершения в каталоге появится файл:

```text
skdf_traffic_accidents_2024-11.csv
```

---

## 📊 Структура данных

CSV содержит (в зависимости от выбранных столбцов):

* Адрес ДТП
* Дата и время
* Код СКДФ
* Вид ДТП
* Ранено
* Погибло
* Количество участников
* Количество ТС
* Система-источник
* Причины ДТП
* `_page` — номер страницы таблицы (служебное поле)

---

## 🧠 Технические особенности (важно понимать)

### Почему Playwright, а не requests

* Данные загружаются динамически
* Фильтры и пагинация работают через React
* Календарь — `react-datepicker` (DOM-события, не input)

### Как выбирается период

1. Открывается фильтр
2. Раскрывается аккордеон **«Период»**
3. Клик по input
4. Календарь:

   * листается стрелками
   * дата начала → клик
   * дата конца → клик
5. Нажимается кнопка **«Показать»**

### Почему нет абсолютных XPath

* Разметка динамическая
* Используются:

  * CSS
  * `:has-text()`
  * role-based селекторы
* Код устойчив к сдвигам DOM

---

## 🧯 Типичные проблемы

### ❌ Календарь не листается

Обычно связано с:

* неправильной локалью
* изменением текста месяцев
  ➡️ Проверь словарь `MONTHS_RU` в коде

### ❌ Таблица не обновляется

* Убедись, что период реально содержит данные
* Попробуй `HEADLESS = False` и посмотри глазами

---

## 🧪 Рекомендации для отладки

```python
HEADLESS = False
```

Также можно добавить:

```python
page.screenshot(path="debug.png")
```

---

## ⚠️ Дисклеймер

Код написан **под текущую разметку сайта**.
Если СКДФ изменит:

* структуру фильтров
* календарь
* пагинацию

— селекторы нужно будет обновить.

---

## 👤 Автор

Скрипт разработан в учебно-аналитических целях.
Подходит для воспроизводимого сбора данных и дальнейшего анализа.

---

Если хочешь — следующим шагом можем:

* вынести календарь в отдельный модуль
* добавить retry / логирование
* переписать на async
* или заменить DOM-парсинг на перехват API (если оно есть)
